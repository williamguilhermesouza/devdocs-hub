[project]
name = "devdocs-hub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "williamguilhermesouza", email = "williamguilhermesouza@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "httpx>=0.28.1",
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
]

[project.scripts]
devdocs-hub = "devdocs_hub:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx2>=2.12.0",
    "hypothesis>=6.165.10",
    "pytest>=9.1.1",
    "ruff>=0.16.4",
]

[tool.fastapi]
entrypoint = "devdocs_hub.api.main:app"

